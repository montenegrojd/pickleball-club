# Authentication Secret Keys
# IMPORTANT: Copy this file to .env.local and change these values!

# The shared key that players use to authenticate
# Example: http://localhost:3000/auth?key=TUESDAY_NIGHT_2024
AUTH_SECRET_KEY=TUESDAY_NIGHT_2024

# Database Configuration
# Set to 'true' to use Firestore, 'false' (or omit) to use JSON file storage
USE_FIRESTORE=false

# Firestore Configuration (only needed if USE_FIRESTORE=true)
# For local development with emulator:
# FIRESTORE_EMULATOR_HOST=localhost:8080
# GOOGLE_CLOUD_PROJECT=pickleball-dev

# For local development with real Firestore:
# GOOGLE_CLOUD_PROJECT=your-dev-project-id
# GOOGLE_APPLICATION_CREDENTIALS=./service-account-dev.json
